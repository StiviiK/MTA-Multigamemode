NEW PASSWORD SECURITY:
       USER INPUTS PASSWORT     CLIENT ENCODES AND HASH THE PASSWORT     SEND TO SERVER            SERVER ENCODES THE PW AGAIN       SELECT DB:HASH FROM DATABASE AND COMPARE
:(USER) PASSWORT => (CLIENT) CLIENT_HASH = sha256(tea(PASSWORT, PRIVATE_KEY)) => (SERVER) SERVER_HASH = sha256(tea(CLIENT_HASH, (DATABASE):PUBLIC_KEY)) => (DATABASE):HASH ?= SERVER_HASH