

-- function CopsnRobbers:CreatePlayerBlip(player)
	-- if  type(player) == 'userdata'  then
		-- player.PlayerBlip = createBlipAttachedTo(player, 0, 2, r, g, b)
	-- end
-- end



-- function CopsnRobbers:DestroyPlayerBlip(player)
	-- if player.PlayerBlip and isElement(player.PlayerBlip) then
		-- destroyElement(player.PlayerBlip)
	-- end
-- end
